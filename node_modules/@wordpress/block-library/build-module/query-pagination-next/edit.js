import { createElement, Fragment } from "@wordpress/element";

/**
 * WordPress dependencies
 */
import { __ } from '@wordpress/i18n';
import { useBlockProps, RichText } from '@wordpress/block-editor';
export default function QueryPaginationNextEdit(_ref) {
  var label = _ref.attributes.label,
      setAttributes = _ref.setAttributes;

  var placeholder = __('Next Page');

  return createElement(Fragment, null, createElement("div", useBlockProps(), createElement(RichText, {
    tagName: "a",
    "aria-label": __('Next page link'),
    placeholder: placeholder,
    value: label,
    allowedFormats: ['core/bold', 'core/italic'],
    onChange: function onChange(newLabel) {
      return setAttributes({
        label: newLabel
      });
    }
  })));
}
//# sourceMappingURL=edit.js.map