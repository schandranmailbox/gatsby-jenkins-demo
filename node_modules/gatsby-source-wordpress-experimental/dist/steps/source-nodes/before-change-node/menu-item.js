"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

exports.__esModule = true;
exports.beforeChangeMenuItem = beforeChangeMenuItem;

var _atob = _interopRequireDefault(require("atob"));

function beforeChangeMenuItem(api) {
  const {
    remoteNode
  } = api;
  const decodedId = (0, _atob.default)(remoteNode.id); // this MenuItem has an incorrect ID.
  // this is a WPGQL bug that will be fixed soon.

  if (decodedId.includes(`post`)) {
    return {
      // for now discard this node
      cancelUpdate: true
    };
  }
}
//# sourceMappingURL=menu-item.js.map